/**
 * SMTP send helper (Nodemailer).
 *
 * Implemented in PR-04. Will expose a `sendEmail({ to, subject, html, text })`
 * function and a typed registry of transactional templates loaded from
 * `src/content/contentConstants.json`.
 */
export {};
