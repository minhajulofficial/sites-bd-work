/**
 * Server-side session helpers.
 *
 * Implemented in PR-04. Will expose `requireUser()`, `requireAdmin()` and a
 * typed `getSession()` accessor that the (user) and (admin) route groups can
 * use through middleware.
 */
export {};
